!!!
%html
  %head
    %title Treefinger
    = javascript_include_tag "application"
    = stylesheet_link_tag    "application"
    = csrf_meta_tags
    = yield :head
  %body
    %header#main
      %section.session
        You are signed in as #{current_user.username}
        |
        = link_to 'view all projects', dashboard_path(current_user.username)
        |
        = link_to 'sign out', :sign_out
      %h1= link_to "TreeFinger", root_path
      = yield :project
    %section#content
      = yield
